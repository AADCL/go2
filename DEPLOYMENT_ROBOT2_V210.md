# 第二条狗：V2.1.0 离线建图导出更新

来源：AADCL/go2 main，提交 `386036437cc2fde3fbbce1f8bdae8b864873a6e1`。
对比基线：V2.0.1，`7d01bb13ab57b87eb31ee43ed511423d12f8f3e5`。

## 主要差异

本次改动集中在 `src/go2_terrain` 的离线地图导出，不是重新更换 FAST-LIO。

| 部分 | 原版 | 新版 |
| --- | --- | --- |
| 地面候选 | ProgressiveMorphologicalFilter，轨迹追踪及相邻格增长 | ApproximateProgressiveMorphologicalFilter、分布式候选、局部鲁棒平面和起点连通表面 |
| 扫描空隙 | 轨迹追踪容易在稀疏条纹断开 | 有限空隙重建及坡顶/坡脚自适应支撑 |
| 二维占据图 | 保留旧绝对 Z 投影结果 | 与坡度层使用同一地面模型，按相对地面高度分类 |
| 墙与天花板 | 墙边缺地面可能漏掉墙点 | 结合邻域可靠地面及连续竖直证据保墙，断开屋面不作为地面 |
| 质量验收 | 轨迹地面追踪覆盖率等旧指标 | 地面覆盖 ≥80%、PGM 自由轨迹 ≥95%、起点自由连通 ≥95% |
| 保存 | 暂存和校验 | 增加事务恢复目录和独立质量报告 |

新文件 `terrain_quality.yaml` 与成套地形资产一起校验；元数据仍为
version 1，增加 `reconstruction_revision: 2`。没有降低质量门槛。
第二条狗的 IP、外参、控制修复、经典步态请求和 enable 优化保持原有适配。

## 操作

工作目录仍是 `/home/unitree/go2_nav_ws`，日常指令不变：

```bash
cd ~/go2_nav_ws
run_go2 mapping 新地图名
# 完成遥控采集后，另一终端：
run_go2 save-map
# 建图终端 Ctrl+C，等待退出，然后：
run_go2 export-map 新地图名
cat maps/新地图名/terrain_quality.yaml
```

已有 PCD、轨迹和有效快照的失败地图可以重新导出，无须先重新采集。
PGM、YAML 和地形层必须成套使用。新版仅改善离线重建，未修改实时
`/terrain/healthy` 高度限制；此前实时雷达高度检查问题不由此更新解决。

## 备份与验证记录

机器人上 `/home/unitree/robot2_terrain_v210_20260910/before.tar.gz`
保存更新前地形包、入口/校验脚本以及今天两张失败地图。
同目录保存构建、测试和导出日志。恢复时应恢复完整配套源码并重新编译，
地图也应恢复成套资产，不能只替换 PGM。

师兄的原始发布说明见 `docs/RELEASE_NOTES_V2.1.0.md`，其中机器狗 1 的
验证结果不是第二条狗的实测结果。第二条狗本次结果以部署日志为准。

第二条狗使用新版默认参数，通过正式 `run_go2 export-map` 导出结果：

| 地图 | 完整命令耗时 | 轨迹地面覆盖 | 自由/起点连通比例 |
| --- | --- | --- | --- |
| lab_202609101747 | 15.794 秒 | 100% | 100% / 100% |
| lab_202609101805 | 15.354 秒 | 100% | 98.8327% / 98.8327% |

旧版分别在 66.5% 和 69.6% 的轨迹追踪覆盖率处失败。新版指标定义有所
变化，上表不是整张地图的准确率；原始 PCD 和轨迹未更改，也未降低门槛。
两张新地图均通过 revision 2 校验（各 16 项哈希），原始点云和轨迹快照
校验通过；旧 `lab_202609091725` 通过 revision 1 兼容校验。
地形包 103 个实际测试用例通过（44 model、31 algorithms、12 surface/
transaction、16 Python validator）；工作区检查通过。已查看两张输出 PGM，
可见自由区、障碍及外围未知区，但未作现场几何验收。现场使用前仍需结合
真实墙线、通道和定位检查。本次没有启用底盘或发出运动目标。
