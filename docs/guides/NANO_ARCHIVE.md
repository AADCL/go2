# 第二条狗：备份与测试记录归档

2026-09-12 将 `/home/unitree` 下我们创建的 20 项历史目录和 Git 离线包归入：

```text
/home/unitree/go2_archive/
├── README.md       # 分类与原路径对应表
├── inventory.json  # 逐项迁移记录与文件元数据校验
├── backups/        # 2 项工程整体备份
├── changes/        # 10 项修复备份、编译与验证日志
├── tests/          # 6 项录包、回放与 SDK 分析
└── git_bundles/    # 2 个独立 Git 离线包
```

合计 4,621 个普通文件，逻辑大小约 2.11 GB（1.97 GiB）。同一文件系统内移动，逐项核对 inode、大小、修改时间、权限与符号链接，全部一致，没有删除或重写归档内容。

日常工程仍为 `/home/unitree/go2_nav_ws`，地图仍位于其中的 `maps/`。运行程序、配置和启动命令没有因归档改变；其他工程、系统配置及 `.ros` 日志目录保持原位。检查未发现活动进程占用这些归档项目，也未发现运行入口或依赖符号链接指向它们。

本仓库修复说明中的备份、录包和回退路径已更新。归档内部的日志、旧源码及旧编译缓存保留原始内容，其中可能仍出现归档前的绝对路径；复现或回退时按 `README.md` 的映射查找文件，不直接运行已移动的旧编译工作空间。

最近一次退出停车修复的备份及验证记录现在位于：

```text
/home/unitree/go2_archive/changes/shutdown_stop_fix_20260912_PGf5TR/
```

其中 `before.bundle` 保留修改前完整源码历史，`before.tar.gz` 保留原 bridge 源码、回归脚本及可执行文件。停止导航并保留新改动后再进行回退，操作细节见 [退出停车修复](../fixes/SHUTDOWN_STOP_FIX_20260912.md)。

今后新建修复记录、录包或备份时，放入对应分类，并保留日期与用途名称。
