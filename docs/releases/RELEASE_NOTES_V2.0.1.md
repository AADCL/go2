# GO2 V2.0.1 发布说明

V2.0.1 是 V2.0.0 的发布校验补丁，不改变建图、重定位、规划、Terrain Guard 或底盘控制算法。

## 修正

- 将 `validate_workspace.sh` 的地形导出基底自由区门限断言从旧值 `0.10` 同步为最终值 `0.08`。
- 将 Terrain Guard 地面覆盖扇区断言从旧值 `4` 同步为机器狗 1 实测采用的 `2`。
- 清理后重新验证活动工作空间，确认不依赖已删除的 `/home/nvidia/go2_mid360_nav`。

## 验证

- `validate_workspace.sh`：通过。
- `lab_202609091725` 地形资产校验：通过。
- V2.0.0 功能源码测试结果保持为 213 项、0 错误、0 失败。

V2.0.0 标签继续保留原始功能快照；V2.0.1 是部署与克隆时推荐使用的版本。
